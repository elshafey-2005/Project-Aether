name: projectaether
description: "High-concurrency MMORPG system simulation - Project Aether."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.4.9
  synchronized: ^3.1.0
  cloud_firestore: ^4.14.0
  firebase_core: ^2.24.2
  rxdart: ^0.27.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  fake_cloud_firestore: ^2.5.0

flutter:
  uses-material-design: true
